import { connect } from 'react-redux';
import { compose } from 'recompose';

import {{properCase name }}Screen from '../screens/{{properCase name }}Screen';

export default compose(
  connect(
    state => ({

    }),
  ),
)({{properCase name }}Screen);
